# Changelog

## [0.3.2] - 2026-09-01

### 变更

- **PDF 导出改用 DevTools 协议驱动浏览器**，不再用 `--print-to-pdf` 命令行开关。换掉的原因是后者在当前 Chrome 上**写完 PDF 后进程不退出**（试了 7 组开关组合，30s 内无一退出），只能靠轮询文件大小 + SIGKILL 这种猜测式收尾；另外它关不掉背景打印、控制不了页面尺寸、也没有等待 webfont 就绪的手段
  - 走 `--remote-debugging-pipe`（fd 3/4 上的 NUL 分隔 JSON），不是 `--remote-debugging-port`：后者需要 WebSocket 客户端，而 Node 直到 v22 才有全局 `WebSocket`，`engines.vscode` 的下限 1.90 用的是 Node 20。协议客户端手写在 `src/export/pdf.ts` 里，零新增依赖，也不开监听端口
  - HTML 经 `Page.setDocumentContent` 直接灌进空白 frame，不再落临时文件
  - 打印前等 `document.fonts.ready` 加两帧，KaTeX 的字形不会再用回退字体的度量排版
  - `printBackground: true` + `preferCSSPageSize: true`：前者是根画布底色能打出来的唯一开关（只有 CSS 的 `print-color-adjust: exact` 盖不住它），后者让 `@page` 继续是页面尺寸的唯一出处
  - 每次启动用一次性 profile 目录：共用 `--user-data-dir` 的第二个进程会因 profile 锁以 21 退出且不产出 PDF
  - 收尾是 `Browser.close` → 等 exit → 超时 SIGKILL，避免泄漏一个用户看不见的 ~250MB headless 进程
- 每次导出冷启动浏览器（约 2s），不常驻。常驻能省下约 1s，代价是编辑器里长期挂一个 249MB 的进程，不划算

## [0.3.1] - 2026-09-01

### 变更

- **双击预览跳到源码改为默认关闭**（`markdownAtlas.preview.doubleClickToSwitchToEditor` 默认 `false`）。双击在网页里的默认含义是选中一个词，抢掉这个手势会让预览里的文字没法正常复制。跳回源码仍可用工具栏的 `←` 按钮，或把该设置打开

## [0.3.0] - 2026-09-01

### 新增

- **PDF 导出**：调用机器上已安装的 Chrome / Edge / Chromium 的 headless `--print-to-pdf`，不打包任何浏览器二进制。自动探测常见安装位置，找不到时提示配置 `markdownAtlas.export.chromePath`
  - `markdownAtlas.export.pdf.pageSize` / `.margin` 经 CSS `@page` 生效
  - 导出的打印样式带 `print-color-adjust: exact`，否则主题的引用块和代码块底色会被打印流程丢掉
  - PDF 的中间 HTML 位于临时目录，相对图片路径无法解析，因此强制内联图片（忽略 `export.embedImages`）
- **工具栏导出下拉**：`💾 Export ▾` 内选 HTML / PDF，并附**目标路径输入框**（绝对路径或相对 md 文件；目录沿用文档名，文件名直接采用且扩展名强制匹配格式）。输入框只影响本次导出，默认值取自 `markdownAtlas.export.outputPath`
- 命令 `Export…`（选格式）、`Export as PDF`；资源管理器右键改为 `Export…`
- **新图标**：编辑器标题栏的预览按钮换成青绿实心圆角方块 + 白色四角罗盘星（深浅主题各一版），不再与内置 Markdown 预览共用 `$(open-preview)`；同一图形做了 256×256 的市场图标
- `package.json` 补上 `icon` 与 `galleryBanner`

### 变更

- 导出不再弹保存对话框，改为直接写入解析出的路径并覆盖同名文件（通知里显示完整路径）—— 与路径输入框的设计一致
- 导出的 HTML 带上 `<!-- Generated by Markdown Atlas -->` 标记

## [0.2.0] - 2026-09-01

从 Markdown2Anything 补齐目录、自定义样式、导出和整排工具栏按钮。

### 新增

- **目录面板**：标题树、点击跳转、滚动时高亮当前标题；与样式面板共用右侧栏，同时只开一个
- **自定义样式面板**：面板内写 CSS 覆盖主题，`Apply` 即时生效并写入 `markdownAtlas.customCss` 设置（重启保留、所有预览共享、导出时一并内联），附常用选择器速查
- **导出独立 HTML**：命令 `Markdown Atlas: Export as HTML`、工具栏 `💾 Export`、资源管理器右键三个入口。CSS 全部内联，本地图片转 data URI，仅当文档使用公式时才附带 KaTeX 字体
  - `markdownAtlas.export.outputPath` 留空时弹保存对话框（覆盖确认交给系统）；配置了路径则直接写入
  - `markdownAtlas.export.embedImages` 控制图片是否内联
- **宽表格显示模式切换**：列内横向滚动 / 完整展开，文档无表格时按钮隐藏，状态存进 `markdownAtlas.preview.tableDisplay`
- **两个同步按钮**：`→` 预览跳到编辑器光标行，`←` 编辑器跳到预览当前位置（都绕过滚动回声锁）
- **原生查找**：面板启用 `enableFindWidget`，`Ctrl/Cmd+F` 直接用 VS Code 自己的查找组件
- 命令 `Markdown Atlas: Edit Custom CSS`；工具栏改为可换行布局，高度由 `ResizeObserver` 实测

### 变更

- 样式表拆成 `base.css`（文章本体，导出会内联）与 `chrome.css`（预览外壳，导出不带），导出文件里不再有匹配不到的规则
- 只改主题 / 自定义 CSS / 外观类设置时不再整篇重渲染，交给 webview 自己应用

## [0.1.0] - 2026-09-01

首个版本。

### 新增

- 自建 webview 预览面板，按文档绑定；`Open Preview` / `Open Preview to the Side` 两个命令，编辑器标题栏按钮、右键菜单和 `Cmd+K A` / `Ctrl+K A` 快捷键
- 8 套主题：`editor`（跟随编辑器配色）、`wechat`、`macos`、`academic`、`notion`、`medium`、`tech-blog`、`clean-blue`，工具栏下拉或 `Select Preview Theme` 命令切换
- 基于 markdown-it 的渲染管线，块级 `data-line` 源码映射：段落、标题、列表项、引用、代码块、表格、分隔线、图片
- 双向滚动同步（可分别开关）与双击回源
- KaTeX 公式（`$`、`$$`、` ```math `），字体随扩展打包
- highlight.js 代码高亮，亮/暗两套 token 配色，固定亮色主题强制使用亮色配色
- 代码块 macOS 三色点标题栏、语言标签、复制按钮
- 单图段落自动转 `<figure>`，alt 文本作为图注
- 宽表格在正文列内横向滚动
- YAML front matter 隐藏或渲染成元信息卡片，且不影响源码行号映射
- 大纲面板、缩放、正文字号与栏宽设置
- `WebviewPanelSerializer`：重启 VS Code 后预览面板与滚动位置自动恢复
